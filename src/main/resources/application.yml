spring:
  application.name: lc-app

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://dm-nlb-112319415-f8e0a97d0b99.kr.lb.naverncp.com:3306/defect_detection_db
    username: ${MARIADB_USERNAME}
    password: ${MARIADB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect
    show-sql: true
    open-in-view: false


logging.level:
  org.hibernate:
    SQL: debug
    type.descriptor.sql: trace